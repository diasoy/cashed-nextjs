import { Rosarivo, Poppins } from "next/font/google";

export const items = Poppins({
    subsets: ['latin'],
    weight: "400"
});
export const title = Rosarivo({
    subsets: ['latin'],
    weight: "400"
});